/** @type {import('next').NextConfig} */
// NOTE: next.config.ts is the primary config file.
// This file is intentionally left minimal to avoid conflicts.
// Do NOT add 'output: export' here — it breaks dynamic routing and Firebase SSR.
const nextConfig = {}

module.exports = nextConfig
